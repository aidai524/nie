node_modules/
*.log
.DS_Store
.superpowers/
ui/

# 真实配置可能含 Slack webhook，永不提交
config.json

# 活库不入库：它每分钟都在变，跟进去会在历史里一次次堆完整副本。
# 想留一份数据就**另存为带日期的快照**：data/snapshots/<name>-YYYY-MM-DD.db
# （git 会把它记成新文件，只占一份存储；活库保持忽略）
data/monitor.db
data/*.db-wal
data/*.db-shm
