# 交接文档

给接手这个仓库的人。目标：让你在半小时内能跑起来、看懂为什么这么写、知道哪些地方不能乱动、以及下一步该做什么。

- **这是什么**：一个常驻服务，定期对 NEAR Intents 的报价接口批量询价，落盘成时间序列，按规则判定异常并推 Slack，同时提供只读 HTTP API 与一个监控面板。
- **当前状态**：`main` 分支，66 个提交，**281 个测试全绿**，**零运行时依赖**（只用 Node 内置模块）。
- **运行前提**：Node.js >= 24。

---

## 1. 半小时上手

```bash
cp config.example.json config.json
# 把 config.json 里的 slack.webhookUrl 填上，或先把 slack.enabled 改成 false
npm test                      # 281 个用例，应全绿且输出干净
npm run once -- --no-notify   # 跑一轮（首次会顺带跑一次深度扫描，约 2 分钟）
npm start                     # 常驻：每分钟一轮采集 + 每 15 分钟深度扫描 + HTTP API
```

然后浏览器打开 `http://127.0.0.1:8787/`，或者：

```bash
curl -s localhost:8787/health | head -c 300         # 活不活、采集陈不陈旧
curl -s "localhost:8787/latest?status=error"        # 现在哪些币对走不通
curl -s "localhost:8787/stats?window=24h"           # 成功率与价格分位
curl -s localhost:8787/depth                        # 最近一次金额档位扫描
```

命令行参数只有四个：`--once`、`--no-notify`、`--config <path>`、`--data <path>`。

---

## 2. 系统地图

```
src/
  config.js     配置读取 / 深合并默认值 / 校验（含 depth 段）
  assets.js     两个 token 列表归一化 + assetId 解析 + 白名单绑定（失败启动即报全）
  amount.js     金额换算：EXACT_OUTPUT 用目标 token 的 decimals
  numeric.js    median / percentile
  http.js       fetchJson（超时 + 错误归类）+ mapLimit（有界并发）
  store.js      SQLite：7 张表、读写、小时聚合、保留策略   ← 最大的文件
  detect.js     异常判定状态机（纯函数，不碰库/网/时钟）
  quote.js      报价请求构造、响应解析、错误归类（limits）
  depth.js      档位折算（BigInt 精确整数运算）+ 深度汇总（纯函数）
  notify.js     Slack：抑制策略、消息格式化、发送
  server.js     只读 HTTP API + 静态文件白名单
  index.js      唯一的装配点：采集循环、维护任务、深度扫描、信号处理
public/
  index.html    面板标记与样式（10 列）
  dashboard.js  纯函数区（可测）+ init() 装配区（靠手工冒烟）
```

**一轮的数据流**（`npm start` 的主循环）：

```
runRound(ctx)                       ← 每分钟
  1. 先读全部币对的近 1 小时历史
  2. quoteAll：并发 5，全部 dry:true
  3. insertQuotes 落盘
  4. evaluate：硬失败 / 相对中位数偏离 → 状态与事件
  5. 边沿触发推 Slack（状态迁移必推；持续异常每 30 分钟提醒一次；恢复必推）
  6. upsertPairState

runMaintenance(ctx)                 ← 每轮都调用，内部各自门控
  ├─ 跨过整点：小时聚合 + 清理过期原始数据（含深度数据）
  ├─ 到点（默认 15 分钟）：runDepthSweep —— 5 个金额档位 × 全部币对
  └─ 到点（本地时间 9 点）：Slack 日汇总（含一行深度快照）
```

---

## 3. 数据模型

| 表 | 内容 | 保留 |
|---|---|---|
| `pairs` | 白名单解析结果（含 decimals、配置金额） | 永久；移出白名单只是 `enabled=0`，历史不删 |
| `quotes` | 哨兵每轮每对一行 | 14 天（`retention.rawDays`） |
| `pair_state` | 每对当前状态（`ok`/`deviant`/`error`） | 永久 |
| `alerts` | 每条事件一行（**每轮都写，不只是推送时**） | **无保留策略，见 §7 FO-1** |
| `quotes_hourly` | 按小时聚合（哨兵） | 永久 |
| `depth_quotes` | 深度扫描每轮每对每档一行 | 14 天，与哨兵同策略 |
| `meta` | `rolled_up_to_hour`、`last_sweep_ts`、`last_digest_ts` | 永久 |

金额字段一律 **TEXT**：它们是链上最小单位的任意精度整数，超过 `Number` 安全范围。唯一允许转成浮点的地方是 SQL 里的 `CAST(... AS REAL)`（聚合统计）。

---

## 4. 设计决策与理由（这些不能随便推翻）

每一条都附「推翻它的代价」，因为每一条都是踩过坑之后定的。

| 决策 | 理由 | 推翻的代价 |
|---|---|---|
| **报价永远带 `dry: true`** | 不生成 deposit address，不在对方系统留状态；这是无人值守可接受的前提 | 监控开始产生链上副作用 |
| **`EXACT_OUTPUT` 的 `amount` 是目标 token 的最小单位** | 参考实现用源 token 的 decimals 换算 —— 只在两边小数位相同时碰巧正确 | 所有跨小数位币对的 amount 都算错 |
| **基准必须在本轮写入之前读取** | 否则当前样本进入自己的基准，每次偏离被自己抹平一个样本，慢漂移永远不告警 | 偏离检测在长时间尺度上失效 |
| **状态列取服务端的 `stateStatus`，面板不自己判定** | `priceDeviationPct` 与 `minSamples` 都不在 API 里；页面硬编码就会与告警说法不一致 | **面板开始骗人**，比没有面板更糟 |
| **深度数据独立建 `depth_quotes` 表** | `/latest` 的实现是 `MAX(id) GROUP BY pair_id`。混进 `quotes` 后，某对的「最新一条」可能变成 1M 档的失败行 | 面板把一条好路由显示成坏了 —— 静默的错误 |
| **档位按名义美元定义，折算用 BigInt** | 两个 token 的小数位不同时，最小单位相除毫无意义（实测 `bsc:USDC` 18 位 → `near:USDC` 6 位得 100110509950192%）；而 18 位小数加上百万档位会超过 `2^53`，超过 `1e21` 时 `String()`/`toFixed()` 都产出 `"1e+21"` | 跨币对不可比；API 拒收非法 amount |
| **大额档不通不报警** | 那是**稳定的路由特征**（实测 `1M` 档 0/33 常年如此）。接进告警会天天响 | 精心设计的「只在变化时响」被噪声废掉 |
| **深度扫描阻塞主循环** | 并发跑会让两条路径同时打 API、同时写库，换来的只是哨兵不迟到 | 多一套并发协调；现在每 15 轮有 1 轮哨兵推迟约 2 分钟 |
| **零运行时依赖** | 部署简单、无供应链风险、`npm test` 直接可跑 | 得自己写 HTTP 客户端、SQLite 封装、测试框架用法、千分位格式化 |
| **所有金额/时间戳是字符串** | 精度与跨时区确定性 | 每个算术处都要显式转换 |

---

## 5. 实测结论（设计依据，不是可选项）

改档位表、改默认金额、或怀疑「是不是配错了」之前，先看这些。

**成本曲线是 L 形带尾巴**（`EXACT_OUTPUT`，档位 = 想收到的美元数）：

```
near:USDC>eth:USDC   100 → 0.41%   1k → 0.14%   10k → 0.11%   100k → 0.11%   1M → No liquidity
near:USDC>bera:USDT  100 → 0.29%   1k → 0.29%   10k → 0.29%   100k → No liquidity
near:USDC>zec:ZEC    100 → 0.58%   1k → 0.26%   10k → 0.24%   100k → 0.26%   1M → No liquidity
arb:USDC>near:USDC   100 → 0.11%   1k → 0.11%   10k → 0.11%   100k → 0.11%   1M → No liquidity
```

1. **小额被固定手续费吃掉**（eth 上 100 美元要 0.41%，1 万美元只要 0.11%），中段最便宜，大额开始变贵、再大**直接没有报价**。所以「大额更吃亏」只是后半段；只测大额会得出错误结论。
2. **断点逐路由差异极大**：`bera` 在 10k~100k 之间断，`eth` 能到 100k 但 1M 断。固定档位表只能粗测，价值在于**跟踪断点往哪边移动**。
3. **失败形态很干净**：大额是 `No liquidity available` 这个明确的 400，不是报价变差。
4. 稳定币之间当前的常见成本是 **0.11%**。

**网络状态几小时内就会变。** 写设计时的实测是：`→bsc:USDC` 报最低额错误、`→tron:USDT` 报 `Internal server error`；几小时后的验收里 `→bsc:USDC` 已恢复、`→xlayer:USDC` 新出现 `NO_QUOTE`。**报红绝大多数不是你的配置问题。**

**其他实测事实**：

- 部分目标链有 **$1,000 的最低额**限制（按美元计），所以稳定币默认金额是 1500 而不是 100
- StableFlow 的 45 个 token **全部**能在 1Click 列表匹配出 `assetId`，代码里的 fallback 拼接规则实测一次都没命中
- 单次报价延迟 0.8–3.2 秒；38 对一轮约 14–17 秒；深度扫描 165 次请求约 129 秒

---

## 6. 运维要点

**判断服务活没活**：看 `/health` 的 `ok`。它在「最近一轮超过 3 个采集周期没完成」时返回 503 —— 同时 `lastRoundAgeMs` 与 `consecutiveRoundErrors` 会告诉你「慢」还是「在报错」。

**这些是正常现象，不要当故障处理**：

- 常年有 **5–6 对报红**（目标链是 `tron` / `xlayer` / `bsc:USDC` 的那几对）
- **`1M` 档 0/33 可通**（当前没有任何路由能吃到百万美元）
- 深度扫描期间日志出现「**上一轮耗时超过 intervalSec，立即开始下一轮**」—— 每 15 轮一次
- 日志出现「**深度扫描跳过 N 对（一小时内没有成功报价，无法折算金额）**」—— 就是上面那几对，哨兵红 → 没有价格 → 无法折算美元，于是跳过

**日志关键字**：`本轮完成: ok=… error=… deviant=…`、`深度扫描完成: N 对 × 5 档 = M 行`、`小时聚合`、`清理 N 条…`。

**数据文件**：默认 `data/monitor.db`（已 gitignore）。原始数据留 14 天，小时聚合永久。深度数据同 14 天、**不做长期聚合**。

**部署**：`deploy/nearintents-monitor.service`（systemd）与 `deploy/Dockerfile` 都在。README 的部署一节有可照抄的步骤。

**配了 `server.bearerToken` 时**：数据端点要令牌，面板会弹输入框（存在浏览器 localStorage）。静态页面本身不校验令牌 —— 否则你连页面都拿不到，也就没机会提交令牌。

---

## 7. 已知问题与后续工作

按建议处理的优先级排。前两条是「真的会咬人」，后面的可以慢慢来。

**FO-1 — `alerts` 表没有保留策略。** 现在的行为是**每一轮、每一对异常都插一行**（不只是推送时），`notified` 标志才是区分。持续故障下约 38 行/分钟。实测库里已经是 469 行且只增不减。修法：给 `alerts` 加保留期，或只在状态迁移时插行。影响：长期运行时库会缓慢膨胀（SQLite 扛得住，但不是好习惯）。

**FO-5 — `listen` 失败会成未捕获异常。** 端口被占用（`EADDRINUSE`）时进程直接抛未捕获异常退出，绕过 `finally` 里的 `store.close()`。**这个我已经实际踩到过**：上一轮验证时留了一个后台进程占着 8787，下一次启动就崩了，且错误信息里看不到「端口被占」这种好懂的提示（systemd 的 `Restart=always` 会把它变成崩溃循环）。修法：给 `server.listen` 加 `error` 事件处理，转成友好的配置错误。

**FO-2 — `parseQuote` 会放行空串 `amountIn`。** 校验用的是 `Number.isFinite(Number(amountIn))`，而 `Number("")` 与 `Number(" ")` 都是 `0`。畸形的成功响应会被记成 `ok:true`（下游 `detect` 会因 `>0` 过滤掉，所以不会污染基准，但会记成绿）。修法：改成正整数正则。

**FO-3 — 极长停机后可能丢未聚合的原始尾段。** 小时桶迭代上限 2000 小时（约 83 天）。停机超过 83 天后重启，「桶上限」与「14 天保留」之间那一段原始数据会被删掉而未聚合。修法：把上限提到覆盖保留期，或在聚合前先检查。

**FO-4 — 窗口内零活动的币对会从 `/stats` 消失。** `getStats` 只输出窗口内有行的币对，所以日报的 `pairCount` 可能少报。稳态下所有对每分钟都在采，碰不到。修法：以 `pairs` 为基准左连接。

**FO-6 — `openStore` 未写 `schema_version`。** `meta` 表已经预留了位置。将来要迁移 schema 时会需要它。

另有约 40 条各任务审查时登记的 Minor（未阻塞合并），共同特点是「代码对、测试或文档弱一点」，例如：若干边界分支没被测试钉住、`clampLimit` 的超限封顶分支无测试、500 错误分支无测试、`Math.min(...values)` 在超大窗口理论上会 RangeError（实际窗口下安全）、几个防御性守卫是死代码。它们散落在三份实现计划各自的 Task 里，需要时按计划的任务标题去搜。

---

## 8. 会让新人踩的陷阱

**1. 面板不能骗人。** 这个项目里已经因此返工两次：

- 「偏离」列显示 `-0.00%`（四舍五入到零却带负号）→ 让人以为数字坏了。改成不留负号，并把列名改为「较基准」说清在跟什么比
- 「可按」列把「**本次没测它**」显示成 `—`（读作「都做不了」）→ 谎报。改成 `?` + 说明未被扫描

写任何展示层代码前先问：**这个数字/符号会不会让人得出错误结论？** 有疑问就补 tooltip 或改措辞。

**2. 测试可以「把被测对象换掉照样通过」。** 真实案例：`/health` 曾经漏掉两个 spec 要求的字段，而 13 轮任务级审查都没发现 —— 因为 `test/server.test.js` 是**用自己的 mock 快照**测 server 的，从来没断言过 `main` 产出的真实快照形状。修法是在 `test/index.test.js` 里起真 `main`、从日志解析端口、打真 `/health`。**看到 mock 就多问一句：真实的那一侧谁在测？**

**3. `String(1e21)` 与 `(1e21).toFixed(0)` 都产出 `"1e+21"`。** 任何要产出「纯十进制整数字符串」的地方（链上金额）都不能过浮点。用 `BigInt`。

**4. 两个 token 的最小单位不能相除。** 小数位不同时结果无意义。要估美元就用 API 给的 `*Usd` 字段。

**5. `/latest` 的语义是 `MAX(id) GROUP BY pair_id`。** 往 `quotes` 表加任何「同一轮多条」的东西都会悄悄改变它的含义。新数据要么独立建表，要么给它加判别列并同步改所有读取路径。

**6. 日汇总的 `hourLocal` 是本地时区**，其他所有时间都是 UTC。这是刻意的例外（操作者的墙上时钟）。

**7. 用脚本改代码时要自检。** 我在这个项目里用脚本应用计划代码，反复犯过同一类错，值得警惕：

- 字符串替换**没有链式**（第二次写入用的是未修改的原始字符串，把第一次替换覆盖回去）
- 用于定位的 needle **命中过宽**（`"depth.intervalSec"` 同时命中测试块和实现块；一个代码块里含两个函数却只打算取一个）
- 追加代码块时 `.rstrip()` 吃掉了首行缩进（语法仍合法，但后续按缩进匹配就失败）
- `git add -A` 把**未提交的他人工作**卷进了自己的提交（用 `git reset --soft` 拆回）
- 手算常数出错（`depthAmountMinor` 的期望值我手算错过一次，用 Python 精确十进制对拍才发现）

结论：脚本改完必须**回读并自检**（导出是否重复、语法是否可加载、计数是否符合预期）。

---

## 9. 如何继续加东西

这个项目走的是 **spec → plan → 预跑验证 → TDD 执行** 的流程，而且**预跑验证这一步真的在回本**。

1. **写设计文档**（`docs/superpowers/specs/`）。**把实测数据写进去** —— 档位表、默认金额、错误码分类都是从实测来的，没有实测依据的设计在这个项目里站不住。
2. **写实现计划**（`docs/superpowers/plans/`）。每个 Task 带完整代码、失败的断言、`Run:` / `Expected:`。计划要细到「改哪一行、锚点是什么」。
3. **预跑验证**（关键一步）：把计划里的代码**整段抽出来，在仓库副本上真实应用并跑测试**。
   这一步在本项目三次共抓出 **6 处真实缺陷**，含：测试 cutoff 落在两行之间、一条数学上不成立的断言、漏了一个 `import`、既有测试夹具缺字段、追加代码块导致重复 import、实现与测试自相矛盾。这些若留到执行期才撞上就是返工。
4. **TDD 执行**，每个 Task 一次提交。**每步都要看着它失败再实现** —— 「先实现再补测试」在这个项目里等于没测（有实例：一条恒真断言 `? out : out` 我写出来过，靠自查删掉了）。
5. **改动面板要人眼验收**。仓库零依赖、没有 DOM 测试框架，渲染层**没有自动化测试**。唯一的自动化保护是 `test/dashboard-dom.test.js`（静态比对 `init()` 要的 id 都在 HTML 里、表头列数）。改 `public/` 之后按 README 末尾那 9 条冒烟清单走一遍。
6. **收尾**：合并前在目标分支上再跑一遍全量测试；用 `--no-ff` 合并保留历史。

**下一步可以做的**（按我的判断排序）：

- 修 FO-5（端口占用崩溃）与 FO-1（`alerts` 膨胀）—— 这两条是真正会咬人的
- 把深度的长期聚合补上（现在 14 天前的深度历史会消失）。要先决定粒度（按天？按档位？）与是否值得
- 如果断点确实会漂，给深度加「变化时才提醒」的边沿告警（现在刻意不做，见 §4）
- 面板加历史趋势视图（`/history` 支持 `res=hourly`，长周期拉小时桶而不是原始行）
- 把 `detect` 的阈值与 `intervalSec` 通过某个端点暴露出来，让面板不必在顶栏硬编码「10%」这个展示值

---

## 10. 文档索引

| 文件 | 内容 |
|---|---|
| `README.md` | 面向运维：快速开始、部署、API 表格、面板与深度扫描说明、**9 条冒烟清单** |
| `docs/superpowers/specs/2026-09-15-near-intents-monitoring-design.md` | 后端设计（含接口实测结论、数据模型、判定规则、保留策略） |
| `docs/superpowers/specs/2026-09-15-quote-dashboard-design.md` | 面板设计（含状态口径、故障与陈旧态、可测性约束） |
| `docs/superpowers/specs/2026-09-15-quote-depth-design.md` | 深度扫描设计（含 L 形曲线实测依据、BigInt 折算、不报警的取舍） |
| `docs/ui-requirements.md` | **面板 UI 的需求清单**（带编号与验证方式）。§4 是两份真实事故换来的「不许说什么」，§12 是明确未满足的需求 |
| `AGENTS.md` | **项目约定**：设计系统护栏、文案规则（含「面板不能骗人」）、改动的验证要求、零依赖约束 |
| `design/clickhouse/DESIGN.md` | 面板遵循的设计系统规范（纯 Markdown）。**改任何界面之前先读它**；规范未覆盖的部分在 `public/index.html` 的 CSS 顶部逐项标为「推导」 |
| `docs/superpowers/plans/*.md` | 三份实现计划，每个 Task 带完整代码与验证步骤。**想知道某处代码为什么长这样，先查这里** |
| `near-intents.html` | 最早的参考实现（浏览器里手填参数打一次报价）。它记录了接口的原始形态，也是那个「用源 token decimals」bug 的出处 |
